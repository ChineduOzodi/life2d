function Camera(position, zoomLevel) {
  this.position = position;
  this.zoomLevel = zoomLevel;
  // this.cameraSpeed = 10;
  // this.cameraZoomZEffect = 1.2;
  // this.cameraZoomSpeed = .001;
}

module.exports = Camera;